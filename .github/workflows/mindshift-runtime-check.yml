name: MindShift Runtime Check

on:
  pull_request:
  push:

jobs:
  notice:
    name: Non-operative notice only
    runs-on: ubuntu-latest
    steps:
      - name: Runtime documentation notice
        run: |
          echo "MindShift runtime check is non-operative."
          echo "No repository files are fetched, changed, approved, proven, or executed by this workflow."
